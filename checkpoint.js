class Checkpoint {
    constructor(){
        this.XP;

    }

    resetPlayer(player){
        player.curHP = player.maxHP;
        
        //enemies killed? loop through array and make visible, set collider
    }
}